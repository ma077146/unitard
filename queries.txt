-- For the client DB:

  // CAREER_SITE_ENABLE 1 = legacy site; 2 = use new career site; 3 = use both.
  $self->runQuery("REPLACE INTO client.customer_config (config_id, value) VALUES (273, 1)");
  
-- For the system DB:

  // New function permission. Add to miscellaneous group (parent: 58).
  $self->runQuery("REPLACE INTO system.user_functions (function_id, code_reference_name, function_name, function_description, is_parent, parent_id, allow_permissions_adjustment, is_active)
    VALUES (72, 'manageCareerSites', 'Manage Career Sites', 'Allow privileged users to add or edit career sites', 0, 0, 0, 1)");

  // Add this as a configuration item that can be enabled for each client.
  $self->runQuery("REPLACE INTO customer_config_default (config_id, parameter, value, name, description, client_configurable_class, is_admin_editable)
    VALUES (273, 'CAREER_SITE_ENABLE', 1, 'Dictates which career site is used.', '1 = legacy site; 2 = use new career site; 3 = use both.', 'YesNoConfigurable', 1);");

  // Put MANAGE_CAREER_SITES in the admin category.
  $self->runQuery("REPLACE INTO customer_config_system_function (config_id, system_function_category_id, is_active) VALUES (273, 9, 1)");
